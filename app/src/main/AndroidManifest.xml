<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FrankyApp"
        tools:targetApi="31">
        <activity
            android:name=".GithubChipActivity"
            android:exported="true"
            android:label="@string/title_activity_github_chip"
            android:theme="@style/Theme.FrankyApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".VocabularyActivity"
            android:exported="true"
            android:label="@string/title_activity_vocabulary"
            android:theme="@style/Theme.FrankyApp">

        </activity>
        <activity
            android:name=".GitUiActivity"
            android:exported="true"
            android:label="@string/title_activity_git_ui"
            android:theme="@style/Theme.FrankyApp" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FrankyApp"></activity>
    </application>

</manifest>